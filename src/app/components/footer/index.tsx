import styles from "./footer.module.scss";

export default function Header() {
  return (
       <section className={styles.footer}>
        <p>A stream of negative content will beat you down and make you apathetic.</p>
       </section>

  );
}